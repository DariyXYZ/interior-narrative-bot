"""Точка входа для Vercel.

Vercel ищет объект `app` в одном из привычных мест (`api/index.py` — одно из
них) и сам оборачивает его в функцию: приложение то же самое, что запускается
локально через uvicorn, никакой отдельной сборки под serverless нет.
"""
from app.api.main import app

__all__ = ["app"]
